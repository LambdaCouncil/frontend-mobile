// @flow

import variables from '../variables/commonColor'

export default _ => {

  const { ui, text } = variables.councils

  return {
    '.note': {
      color: '#8F8E95'
    },
    // width: 90,
    marginRight: -4,
    flexGrow: 1
  }

}
